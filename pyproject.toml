[project]
name = "phd"
version = "0.1.0"
description = "Andrea Martinelli codebase for an eventual PhD"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "ExpRegistry",
]
